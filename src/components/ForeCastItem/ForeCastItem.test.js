// import React from "react";
// import { render } from "@testing-library/react";
// import ForeCastItem from "./ForeCastItem";

// test("ForeCastItem render", async () => {
//   //tarea se deben renderizas los diferentes textos

//   const { findByText } = render(
//     <ForeCastItem
//       weekDay={"Friday"}
//       hour={"16:00:54"}
//       state={"sunny"}
//       temperature={20}
//     />
//   );
//   //findByText= permite encontrar un componente por el texto que muestra

//   // eslint-disable-next-line testing-library/prefer-screen-queries
//   const weekDay = await findByText(/Friday/);
//   // eslint-disable-next-line testing-library/prefer-screen-queries
//   const hour = await findByText(/16:00:54/);
//   // eslint-disable-next-line testing-library/prefer-screen-queries
//   const state = await findByText(/sunny/);
//   // eslint-disable-next-line testing-library/prefer-screen-queries
//   const temperature = await findByText(/20/);

//   expect(weekDay).toHaveTextContent("Friday");
//   expect(hour).toHaveTextContent("16:00:54");
//   expect(state).toHaveTextContent("sunny");
//   expect(temperature).toHaveTextContent(20);
// });

// import React from 'react'

test("ForecastItem render", () => {
  // Tarea, se deben renderizar los diferentes textos
});
